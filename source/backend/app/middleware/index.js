import { Router } from 'express';

/**
 * Creates Router for middleware
 *
 * @returns {Router}
 */
export default ({ config, db }) => {
  let routes = Router();

  // add middleware here

  return routes;
};
